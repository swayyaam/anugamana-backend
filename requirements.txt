# Anugamana — runtime dependencies (direct only).
# Exact transitive closure: requirements.lock.txt
# Dev/test/eval extras:     requirements-dev.txt
#
# Regenerate the lock with:  pip freeze > requirements.lock.txt

# --- API ---
fastapi==0.136.1
uvicorn==0.46.0
gunicorn==25.3.0
pydantic==2.13.3
slowapi==0.1.9

# --- retrieval ---
chromadb==1.5.9
FlagEmbedding==1.4.0
sentence-transformers==5.4.1
transformers==4.57.6
torch==2.11.0
numpy==2.4.4

# --- generation ---
anthropic==0.97.0

# --- infrastructure ---
structlog==25.5.0
tenacity==9.1.4
python-dotenv==1.2.2
